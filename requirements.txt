# AI Podcast Creator - Dependencies

# Core Framework
typer[all]>=0.9.0,<0.15.0
rich>=13.7.0,<14.0.0
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0

# AI/ML Core
torch>=2.1.0,<2.10.0
torchaudio>=2.1.0,<2.10.0
transformers>=4.35.0,<4.50.0
diffusers>=0.24.0,<0.36.0
accelerate>=0.25.0,<1.12.0

# TTS Engines
gTTS>=2.3.0,<3.0.0  # Primary TTS engine (works on all Python versions)
# TTS>=0.22.0,<0.25.0 ; python_version < '3.12'  # Advanced TTS (disabled - numpy conflict with librosa)
# Note: TTS library requires numpy==1.22.0 which conflicts with librosa>=0.10.1
# Using gTTS as primary engine instead. Can be re-enabled if librosa is downgraded or TTS upgraded.
# Optional commercial APIs (uncomment if using):
# elevenlabs==0.2.27
# azure-cognitiveservices-speech==1.34.0

# Audio Processing
pydub==0.25.1
librosa>=0.10.2,<0.11.0  # Upgraded to support numpy 1.23+
soundfile==0.12.1
scipy>=1.11.0,<1.16.0
numpy>=1.23.0,<3.0.0  # Compatible with librosa 0.10.2+ and most ML libraries

# Music Generation (Optional - requires 'av' package which has build issues on Windows)
# Note: May have compatibility issues with Python 3.12+, test before using
# Disabled in CI due to build failures - install manually if needed
# audiocraft>=1.3.0,<1.5.0 ; python_version < '3.12'

# Video Processing
moviepy==1.0.3
opencv-python==4.11.0.86
pillow==10.1.0
imageio==2.33.0
imageio-ffmpeg==0.4.9
face-alignment>=1.4.0  # Installed: 68-point facial landmarks for accurate lip-sync alignment (works with Python 3.13)
# mediapipe>=0.10.0  # Optional: Best facial landmark detection (468 points) - Not available for Python 3.13 yet

# Database
sqlalchemy>=2.0.23,<2.1.0
alembic>=1.13.0,<1.18.0

# Task Queue (Optional)
celery>=5.3.0,<6.0.0
redis>=5.0.0,<6.0.0

# API Framework (Optional)
fastapi>=0.104.0,<0.115.0
uvicorn[standard]>=0.24.0,<0.35.0

# Utilities
python-dotenv>=1.0.0,<2.0.0
requests>=2.31.0,<3.0.0
httpx>=0.25.0,<0.30.0
tenacity>=8.2.0,<10.0.0
loguru>=0.7.0,<0.8.0
tqdm>=4.66.0,<5.0.0
pyyaml>=6.0.0,<7.0.0
psutil>=6.1.0,<8.0.0  # System resource monitoring (CPU, RAM)
# GPU-accelerated video processing (CUDA support) - Optional, install manually if needed
# ffmpegcv[cuda]>=0.2.0,<1.0.0  # Requires CUDA development tools - disabled in CI

# Development
pytest==8.4.2
pytest-asyncio==1.2.0
pytest-cov==7.0.0
pytest-xdist>=3.5.0,<4.0.0
pytest-timeout>=2.2.0,<3.0.0
pytest-benchmark>=4.0.0,<6.0.0
hypothesis>=6.96.0,<7.0.0
black==25.9.0
flake8==7.3.0
mypy==1.18.2
pre-commit==3.6.0

